{
  "name": "cesiumai",
  "private": true,
  "version": "0.0.0",
  "description": "CesiumAI 本地开发脚本",
  "scripts": {
    "dev": "concurrently -k -n api,web -c cyan,magenta \"npm run dev:api\" \"npm run dev:web\"",
    "dev:api": "dotnet run --project backend/CesiumAI.Api",
    "dev:web": "cross-env VITE_API_BASE_URL=http://localhost:5088 npm --prefix frontend run dev"
  },
  "devDependencies": {
    "concurrently": "^9.2.1",
    "cross-env": "^7.0.3"
  }
}
